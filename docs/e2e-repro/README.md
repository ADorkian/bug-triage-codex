# E2E Repro Reports

This directory stores per-issue browser E2E verification reports written by `e2e_reproducer`.

Expected file pattern:

- `{ISSUE_KEY}-playwright-mcp.md`

These reports complement, but do not replace, the main triage artifacts under `artifacts/`.
